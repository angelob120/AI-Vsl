# nixpacks.toml - Railway build configuration
# Installs Node.js AND FFmpeg for server-side video processing

[phases.setup]
nixPkgs = ["nodejs_18", "ffmpeg-full"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"