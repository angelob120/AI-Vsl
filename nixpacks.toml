# nixpacks.toml - Railway build configuration
# This file tells Railway to include FFmpeg for video processing

[phases.setup]
nixPkgs = ["ffmpeg", "nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"