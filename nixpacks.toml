# nixpacks.toml - Railway build configuration
# Installs FFmpeg for server-side video processing

[phases.setup]
nixPkgs = ["ffmpeg-full"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"